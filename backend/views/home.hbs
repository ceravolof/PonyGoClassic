<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/static/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .navbar {
            background-color: #333;
            overflow: hidden;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 15px;
        }
        .navbar .menu-toggle {
            font-size: 20px;
            color: white;
            background: none;
            border: none;
            cursor: pointer;
        }
        .navbar .menu {
            display: none;
            flex-direction: column;
            background-color: #333;
            position: absolute;
            top: 50px;
            left: 0;
            width: 100%;
            z-index: 1000;
        }
        .navbar .menu a {
            color: white;
            text-decoration: none;
            padding: 14px 20px;
            display: block;
        }
        .navbar .menu a:hover {
            background-color: #575757;
        }
        .navbar .logout {
            color: white;
            background-color: #f44336;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
        }
        .navbar .logout:hover {
            background-color: #d32f2f;
        }
        .container {
            padding: 20px;
            text-align: center;
        }
        .welcome-message {
            font-size: 24px;
            margin-bottom: 20px;
        }
    </style>
    <script>
        function toggleMenu() {
            const menu = document.querySelector('.navbar .menu');
            const toggleButton = document.querySelector('.navbar .menu-toggle');
            if (menu.style.display === 'flex') {
                menu.style.display = 'none';
                toggleButton.textContent = '☰';
            } else {
                menu.style.display = 'flex';
                toggleButton.textContent = '✖';
            }
        }
    </script>
</head>
<body>
    <div class="navbar">
        <button class="menu-toggle" onclick="toggleMenu()">☰</button>
        <div class="menu">
            <a href="/home">Home</a>
            <a href="/profile">Profile</a>
            <a href="/settings">Settings</a>
        </div>
        <button class="logout" onclick="window.location.href='/logout'">Logout</button>
    </div>

    <div class="container">
        <div class="welcome-message">
            Welcome back, <span id="username">User</span>!
        </div>
        <p>Use the menu to navigate through the site.</p>
    </div>

    <script>
        // Replace "User" with actual username if needed
        document.getElementById('username').textContent = "John Doe"; // Example username
    </script>
</body>
</html>
